// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
import java.awt.Graphics;
import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;
import java.net.URL;
import javax.imageio.ImageIO;
import javax.swing.JPanel;
import javax.swing.Timer;

class EntryScreen$EntryScreenPanel extends JPanel {
   private BufferedImage backgroundImage;
   private int offset = 0;
   private Timer scrollTimer;
   private int scrollSpeed = 2;

   public EntryScreen$EntryScreenPanel(String var1) {
      this.loadImage(var1);
      this.startScrolling();
   }

   private void loadImage(String var1) {
      try {
         File var2 = new File("Ocean.png");
         if (var2.exists()) {
            this.backgroundImage = ImageIO.read(var2);
         } else {
            URL var3 = this.getClass().getResource(var1);
            if (var3 != null) {
               this.backgroundImage = ImageIO.read(var3);
            } else {
               System.err.println("Image not found: " + var1);
            }
         }

         if (this.backgroundImage == null) {
            System.err.println("ImageIO could not decode: " + var1);
         }
      } catch (IOException var4) {
         System.err.println("IOException while loading: " + var1);
         var4.printStackTrace();
      }

   }

   private void startScrolling() {
      if (this.backgroundImage != null) {
         this.scrollTimer = new Timer(16, (var1) -> {
            this.offset -= this.scrollSpeed;
            this.repaint();
         });
         this.scrollTimer.start();
      }
   }

   public void stopScrolling() {
      if (this.scrollTimer != null) {
         this.scrollTimer.stop();
      }

   }

   protected void paintComponent(Graphics var1) {
      super.paintComponent(var1);
      if (this.backgroundImage != null) {
         int var2 = this.getWidth();
         int var3 = this.getHeight();
         int var4 = this.backgroundImage.getWidth();
         if (var4 > 0) {
            int var5 = this.offset % var4;
            if (var5 > 0) {
               var5 -= var4;
            }

            for(int var6 = var5; var6 < var2; var6 += var4) {
               var1.drawImage(this.backgroundImage, var6, 0, var4, var3, this);
            }

         }
      }
   }
}
