package GameCreation;

import java.util.ArrayList;
import java.util.List;

public class FutureLevelCatalog {
    private FutureLevelCatalog() {
    }

    public static List<LevelData> loadFutureLevels() {
        List<LevelData> futureLevels = new ArrayList<>();

        // TODO: Add additional level definitions here as the game expands.
        // Example:
        // futureLevels.add(LevelData.createStarterLevel(6000, 720));

        return futureLevels;
    }
}
